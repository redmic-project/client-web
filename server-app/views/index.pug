extends layout

block loading
	#loadingContainer
		span.fa.fa-refresh.spinningElement

block root
	#rootContainer

	script.
		require([
			'src/app/App'
		], function(
			App
		) {
			new App();
		});
