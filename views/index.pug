extends layout

block loading
	#loadingContainer
		span.fa.fa-refresh.spinningElement

block root
	#rootContainer

	script.
		require([
			'app/components/Router'
		], function(
			Router
		) {
			new Router();
		});
